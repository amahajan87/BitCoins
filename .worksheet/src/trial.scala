object trial {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(41); 
      var zeroString = "0";System.out.println("""zeroString  : String = """ + $show(zeroString ));$skip(53); 
      for ( i <- 0 to 5-2) {
      zeroString += "0"
    };$skip(30); 
    println(zeroString)}
}
